import 'package:flutter/material.dart';
import 'package:youtubeflutterlist/list.dart';
 
void main() => runApp(MyApp());
 
class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return VideoList();
  }
}